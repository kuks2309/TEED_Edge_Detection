# Claude 작업 지침

## 핵심 원칙 (8대 원칙)
1. **모든 의도와 결정을 즉시 기록한다**
2. **객관적 사실로 판단한다** (코드·로그·매뉴얼 인용, 추측·기억·일반 지식 금지)
3. **모르는 것은 "확인되지 않음" 으로 명시한다**
4. **사용자가 지시한 사항만 수행한다**
5. **관련 이론을 철저히 조사한 후 시작한다** (공식 문서·매뉴얼·기존 코드 우선)
6. **코딩 전 구조를 제시하고 사용자 승인을 받는다**
7. **임의로 기능을 추가하거나 변경하지 않는다**
8. **검증·보고는 파일·줄·실제 출력을 인용한다** ("동일합니다" 금지)

## 현재 프로젝트 상태
- **모델**: CNN 단일 패턴 인식 (EdgeCNN)
- **학습 방식**: Positive 데이터만 사용 (label=1로 학습)
- **Negative 데이터**: 사용하지 않음
- **추론**: CNN 출력 confidence (0~1) - 높으면 학습된 패턴과 유사

## 금지 사항
- Negative 데이터 수집/학습 로직 추가 금지
- Autoencoder로 변경 금지
- 사용자가 요청하지 않은 모델 구조 변경 금지
- 요청하지 않는 코딩 금지
- 코딩 전에 프로그램 구조 먼저 제시하고 코딩은 승인 받을 것

## 코드 수정 규칙
- 기존에 지시한 코드 로직을 임의로 변경하지 않는다
- 이전에 승인받은 코드 구조를 유지한다
- 변경이 필요하면 반드시 사유를 설명하고 승인받는다

## 작업 검증 보고 (증거 기반)
- 완료 보고는 다짐·선언이 아니라 검증 가능한 증거로 한다: 수행 항목마다 파일 경로·줄 번호·실제 실행 출력을 인용한다.
- 증거 없는 "동일합니다"/"완료했습니다" 선언 보고를 금지한다.
- 검증을 수행하기 전에 완료 표시([X])를 미리 채운 보고 양식을 사용하지 않는다.
- 지시받지 않은 변경이 발생했으면 숨기지 말고 변경 목록에 명시한다.
